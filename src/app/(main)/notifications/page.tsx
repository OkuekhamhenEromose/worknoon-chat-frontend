import { NotificationPanel } from '@/components/shared/NotificationPanel';

export const metadata = { title: 'Notifications — Worknoon' };

export default function NotificationsPage() {
  return <NotificationPanel />;
}