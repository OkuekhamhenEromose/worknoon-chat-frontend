import { RegisterForm } from '@/components/auth/RegisterForm';

export const metadata = { title: 'Create Account — Worknoon' };

export default function SignupPage() {
  return <RegisterForm />;
}