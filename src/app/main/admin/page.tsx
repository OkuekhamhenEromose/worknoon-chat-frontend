import { AdminDashboard } from '@/components/dashboard/AdminDashboard';

export const metadata = { title: 'Dashboard — Worknoon' };

export default function AdminPage() {
  return <AdminDashboard />;
}